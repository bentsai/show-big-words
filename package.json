{
  "name": "show-big-words",
  "version": "0.1.0",
  "description": "Static local-first slide editor that presents text as large as possible",
  "private": true,
  "scripts": {
    "test:visual": "npx playwright test"
  },
  "keywords": ["presentation", "slides", "text", "static"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "http-server": "^14.1.1"
  }
}
