# Project notes for Claude

- The whole app is one file: `index.html` (markup + embedded CSS + JS, no build step).
- Tests: `npm run test:visual` (Playwright, served over http-server). Keep them green.
- **Open work lives in [TASKS.md](TASKS.md)** — check it for what to tackle next.
- README.md is the source of truth for current behavior; PLAN.md is a historical artifact.
